apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: authentik
  namespace: flux-system
spec:
  # No targetNamespace: this Kustomization spans both the authentik
  # namespace (the app itself) and the database namespace (its
  # DatabaseRole/Database, which must live alongside the shared Cluster).
  # Every resource here sets its own metadata.namespace explicitly.
  path: ./kubernetes/apps/authentik/authentik/app
  sourceRef:
    kind: GitRepository
    name: flux-system
  prune: true
  wait: true
  interval: 1h
  retryInterval: 1m
  timeout: 10m
  dependsOn:
    - name: postgres
      namespace: flux-system
    - name: gateway
      namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
